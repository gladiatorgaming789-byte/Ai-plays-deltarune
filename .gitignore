.venv/
__pycache__/
*.py[cod]
.pytest_cache/
runs/
memory/

# Mod build intermediates are reproducible and should not dirty a checkout.
mods/speed/.build/
mods/speed/deltamod/*.g3mpatch
mods/speed/deltamod/*.json
mods/speed/deltamod/*.xml

# The two current direct-CSX runtime-test ZIPs are intentionally tracked.
# Start AI GUI.bat verifies their exact release-record size/SHA-256 and rebuilds
# a missing or damaged copy before opening the GUI.

name: Recovery CI

on:
  push:
    branches:
      - development
      - recovery/**
  pull_request:
    branches:
      - development

permissions:
  contents: read

jobs:
  verify:
    runs-on: windows-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install pytest

      - name: Compile all Python files
        run: python -m compileall -q deltarune_agent tests

      - name: Import every package module
        shell: pwsh
        run: |
          @'
          import importlib
          import pkgutil
          import deltarune_agent

          failures = []
          for module in pkgutil.walk_packages(
              deltarune_agent.__path__, deltarune_agent.__name__ + "."
          ):
              try:
                  importlib.import_module(module.name)
              except BaseException as exc:
                  failures.append(f"{module.name}: {type(exc).__name__}: {exc}")
          if failures:
              raise SystemExit("\n".join(failures))
          '@ | python -

      - name: Validate CLI startup
        shell: pwsh
        run: |
          python -m deltarune_agent --help
          python -m deltarune_agent run --help
          python -m deltarune_agent telemetry --help
          python -m deltarune_agent replay --help

      - name: Run tests
        run: python -m pytest -q
